<?php

use Illuminate\Database\Seeder;

class MembershipSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        //here u call all the factory and loop though them (u call also use it to seed data to the database)
        //[we will lok at it late as well]
    }
}
